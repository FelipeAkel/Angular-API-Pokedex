export interface TypePokemonModel {
    value: string;
    name: string;
    class: string;
}

export interface TesteModel {

    id: number,
    code: string,
    name: string,
    description: string,
    image: string,
    price: number,
    category: string,
    quantity: number,
    inventoryStatus: string,
    rating: number,
}